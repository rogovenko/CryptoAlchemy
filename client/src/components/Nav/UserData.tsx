const UserData = ({ health, moves }: { health: number; moves: number }) => {
  return (
    <>
      {/* <span className="p-2 rounded bg-white">HP: {health}</span>
      <span className="p-2 rounded bg-white">Clicks: {moves}</span> */}
    </>
  );
};

export default UserData;